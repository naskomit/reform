import scala.meta._

class SourceTransformer {
  def transform(source_in: Source): Source = {
    source_in
  }
}

object SourceTransformer {
  def apply(): SourceTransformer = new SourceTransformer()
}
